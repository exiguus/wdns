name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read
  packages: write

jobs:
  call-reusable:
    uses: ./.github/workflows/reusable-build.yml
    with:
      publish: false
      image_tags: wdns:ci
